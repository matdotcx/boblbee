#!/bin/bash
# Run the Downloads sweep immediately in the foreground (ignores the 7-day cadence). Same mode as the LaunchAgent unless given --apply / --dry-run.
exec "$HOME/bin/downloads-sweep.sh" "${1:---dry-run}" --force
